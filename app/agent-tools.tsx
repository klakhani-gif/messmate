'use client';
import {useEffect} from 'react';
import {providers} from './providers';
export default function AgentTools(){useEffect(()=>{const context=(document as unknown as {modelContext?:{registerTool:(tool:unknown,options:unknown)=>unknown}}).modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();try{Promise.resolve(context.registerTool({name:'find_mess_services',description:'Read publicly researched Tbilisi mess listings. Prices and availability are unconfirmed.',inputSchema:{type:'object',properties:{query:{type:'string'}},required:['query'],additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:true},execute(input:unknown){if(!input||typeof input!=='object'||typeof (input as {query?:unknown}).query!=='string')throw new Error('query must be a string');const q=(input as {query:string}).query.toLowerCase();return providers.filter(p=>(p.name+' '+p.description).toLowerCase().includes(q)).map(p=>({name:p.name,url:'/mess/'+p.id,source:p.source,status:'unconfirmed'}));}}, {signal:lifecycle.signal})).catch(()=>{})}catch{}return ()=>lifecycle.abort()},[]);return null}
