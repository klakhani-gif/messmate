import {providers} from '../../providers';
import {notFound} from 'next/navigation';
import RequestForm from '../../request-form';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=providers.find(p=>p.id===id);if(!p)notFound();return <main className="form-shell"><a href="/" className="back">← All messes</a><p className="eyebrow">DISCOVERY LISTING · PARTNERSHIP NOT CONFIRMED</p><h1>{p.name}</h1><p>{p.description}</p><a className="primary-button" href={p.source} target="_blank" rel="noreferrer">Open {p.label} ↗</a><div className="detail-grid"><div><h2>Plans & weekly menu</h2><p>Current prices, portions and day-by-day menus have not been confirmed. Ask the provider for its latest offer.</p></div><div><h2>Delivery & trial meals</h2><p>Coverage, delivery times, trial availability and cancellation terms need provider confirmation.</p></div></div><p>Interested in this service? Register interest below. We cannot accept bookings or take payment for this listing yet.</p><RequestForm provider={p.id}/></main>}
